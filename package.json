{
    "name": "crypto-balance",
    "version": "0.1.0",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/Drarig29/crypto-balance.git"
    },
    "description": "A web application to show charts and graphs of your crypto balance",
    "main": "frontend/index.js",
    "scripts": {
        "build:backend": "cargo build",
        "build:frontend": "esbuild --bundle frontend/index.jsx --outfile=static/assets/bundle.js --loader:.js=jsx --minify",
        "build": "npm run build:backend && npm install && npm run build:frontend",
        "dev": "esbuild --bundle frontend/index.jsx --outfile=static/assets/bundle.js --loader:.js=jsx --watch",
        "start": "cargo run"
    },
    "author": "Corentin Girard <corentingirard.dev@gmail.com>",
    "license": "ISC",
    "devDependencies": {
        "esbuild": "^0.11.10",
        "react": "^17.0.1",
        "react-dom": "^17.0.1"
    },
    "dependencies": {
        "css-loader": "^5.2.1",
        "recharts": "^2.0.9",
        "style-loader": "^2.0.0"
    }
}
